#include"main.h"
#include<math.h>
/**
 *_abs-rend- return the abs value of an int
 *@a: the int to convert
 *
 *
 *
 *Return: the "converted" a
*/

int _abs(int a)
{
	if (a < 0)
		return (-a);
	else
		return (a);
}
