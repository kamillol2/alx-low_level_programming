#include"main.h"
/**
 *add-sums up two integers
 *@a: an integer to sum
 *@b: same as a
 *
 * Return: sum which is the addition of both integers
*/
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);

}
